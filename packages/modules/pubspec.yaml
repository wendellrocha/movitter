name: modules
description: A new Flutter package project.
version: 0.0.1
publish_to: none

environment:
  sdk: '>=2.18.0 <3.0.0'
  flutter: ">=2.10.0"

dependencies:
  flutter:
    sdk: flutter
  common:
    path: '../common'
  dependencies:
    path: '../dependencies'

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
